/** Smallest allowed increment for bathroom counts (e.g., 1, 1.5, 2). */
export const BATHROOM_INCREMENT = 0.5;

/** Fallback image used by presale pages when no listing image is available. */
export const PRESALE_IMAGE_FALLBACK = "/vv-asset-2-desktop.png";
